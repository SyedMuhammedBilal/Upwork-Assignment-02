// we currently have only one environment, but we can add more environments here like localhost.
export const APIURL = "https://jsonplaceholder.typicode.com"

export const stagingEnvConfig = {
    serverUrl: APIURL,
}